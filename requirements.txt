# Email RL System - Requirements
#
# Install: pip install -r requirements.txt
# Or with uv: uv pip install -r requirements.txt

# =============================================================================
# PIPELINE REQUIREMENTS (required for run_pipeline.py)
# =============================================================================

# Database
asyncpg>=0.29.0        # PostgreSQL async driver
psycopg2-binary>=2.9.0 # PostgreSQL sync driver (for labeling UI)

# Data processing
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0    # KMeans clustering

# OpenAI API (for embeddings - optional)
openai>=1.0.0

# Web UI (labeling interface)
streamlit>=1.29.0

# Utilities
tqdm>=4.66.0
python-dateutil>=2.8.0

# =============================================================================
# TRAINING REQUIREMENTS (optional - for model training)
# =============================================================================

# Core ML frameworks
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# Apple Silicon optimization (optional)
# mlx>=0.4.0
# mlx-lm>=0.4.0

# Transformers and LLM tools
transformers>=4.36.0
accelerate>=0.25.0
peft>=0.7.0            # Parameter-Efficient Fine-Tuning (LoRA)
# bitsandbytes>=0.41.0 # Quantization (Linux only)

# Reinforcement Learning
trl>=0.7.0             # Transformer Reinforcement Learning (DPO, KTO, PPO)

# Sentence embeddings
sentence-transformers>=2.2.0

# Data processing (extended)
pandas>=2.0.0
pyarrow>=14.0.0        # Parquet support

# NLP
nltk>=3.8.0

# Experiment tracking
wandb>=0.16.0
tensorboard>=2.15.0

python-dotenv>=1.0.0  # Environment variable management

# Hugging Face
datasets>=2.15.0
huggingface-hub>=0.19.0

# =============================================================================
# DEVELOPMENT REQUIREMENTS (optional)
# =============================================================================

ipython>=8.18.0
jupyter>=1.0.0
pytest>=7.4.0
black>=23.12.0
pyyaml>=6.0.0
